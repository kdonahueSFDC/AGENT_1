<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Prevent_Done_Status_Reversal</fullName>
    <active>true</active>
    <description>Prevents changing status from Done back to an earlier status (Backlog, In Progress, In Review, or Blocked). Once a user story is marked as Done, it should not be moved back to a previous status.</description>
    <errorConditionFormula>AND(
    TEXT(PRIORVALUE(Status__c)) = &quot;Done&quot;,
    TEXT(Status__c) != &quot;Done&quot;
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Cannot change status from Done to an earlier status. Once a user story is marked as Done, it cannot be moved back to Backlog, In Progress, In Review, or Blocked.</errorMessage>
</ValidationRule>
